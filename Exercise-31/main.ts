import { userInfo } from "os"

//Question31
let User_names:string[]=[`Bossman`,`Admin`,`KingofStyle`,`Edgy`,`Sandy`]
if(User_names.length == 0){
    console.log("we need to find some users!")
}
else{
User_names=[]
console.log(`All usernames have removed: ${User_names.length}`)
}